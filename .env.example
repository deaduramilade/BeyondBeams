# Server configuration
API_KEYS={"replace-with-a-generated-secret":"enterprise"}
HOST=127.0.0.1
PORT=3000

# Browser dashboard configuration is derived from the hosting origin.
# Never embed privileged credentials in a browser bundle. The prototype
# dashboard requests a development API key interactively.